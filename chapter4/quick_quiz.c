/*Write a program to 
print natural number from 10 to 20 when initial loop
is counter to 0*/
#include<stdio.h>
int main() {
    int a=0;
    while(a<21) {
        if(a>=10){
        printf("%d\n", a);
     
    }
       a++; }
    return 0;
}