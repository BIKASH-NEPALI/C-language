// it's same as using while loop but it's execute first
// and then check the conditon, but while loop first check the conditon then executes then code
#include<stdio.h>
int main() {
    int i=4;
    do{
        printf("you are awesome");
    }while(i>5);
    return 0;
}